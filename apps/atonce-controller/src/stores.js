import { spaceData } from "./spaceData";
import { spaceDataPath } from "./spaceDataPath";
import { SPACE_NAME } from "./constants";

export const songBooks = spaceDataPath(SPACE_NAME, "songBooks", true);
export const channels = spaceDataPath(SPACE_NAME, "channels", true);
//export const privateSpace = spaceData(SPACE_NAME, true);
